# Selenium与Webdriver的关系
* 在早期Selenium1与Webdriver是两个独立的项目
  * selenium主要用于web应用程序的自动化测试的工具，Webdriver是通过原生浏览器支持或浏览器扩展来直接控制浏览器。
* selenium2就把selenium1(RC)和webdriver合并到一起，还是用selenium的名字，但是实现方式和协议基本沿用的是webdriver的。  